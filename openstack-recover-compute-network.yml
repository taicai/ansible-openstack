- name: remove the base yum repository
  hosts: openstack-nova-compute-recover 
  remote_user: feiyu.liu
  sudo: yes
  tasks:
    - name: remove the base yum repository
      shell: rm -f /etc/yum.repos.d/CentOS-*

- name: install openstack common
  hosts: openstack-nova-compute-recover 
  remote_user: feiyu.liu
  sudo: yes
  roles:
    - { role: openstack-recover-os-network,  run_type: "once", manager_interface: enp3s0, data_interface: enp4s0, manager_gateway: 10.100.24.254 }
